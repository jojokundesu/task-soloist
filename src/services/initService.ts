/**
 * Legacy init stub. App initialization is handled by AppProvider + the local API.
 * Service worker registration lives in App.tsx.
 */
export const initializeApp = () => {
  console.log('Task Soloist: using local full-stack API (SQLite).');
};
